setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    api: api/Dockerfile
    client: client/Dockerfile
    nginx: nginx/Dockerfile
  config:
    NODE_ENV: production
release:
  command: 
    - npm run migrate
  image: api   

