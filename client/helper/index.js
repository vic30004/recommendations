import { useQuery, useMutation } from "@apollo/client";
import { GET_USER } from "../src/graphql";

// export const getUserByUsername = (username) => {
//   return ({ data, loading, error } = useQuery(GET_USER, {
//     variables: { username },
//   }));
// };
